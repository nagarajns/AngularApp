'use strict';

AdminApp.controller('AllClassesController', function($rootScope, $scope, $http, $timeout) {
    $scope.$on('$viewContentLoaded', function() {
        Layout.init();

        // initialize core components

    });
});